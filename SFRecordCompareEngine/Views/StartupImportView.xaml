<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="SFRecordCompareEngine.Views.StartupImportView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Loaded="OnLoaded"
    Unloaded="OnUnloaded">
    <Grid Padding="48" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel
            Width="520"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Spacing="16">
            <TextBlock
                Text="Starfield Record Compare Engine"
                FontSize="28"
                FontWeight="SemiBold"
                Foreground="{ThemeResource TextFillColorPrimaryBrush}"
                TextAlignment="Center" />
            <TextBlock
                Text="{Binding StatusText}"
                FontSize="16"
                Foreground="{ThemeResource TextFillColorPrimaryBrush}"
                TextAlignment="Center"
                TextWrapping="Wrap" />
            <TextBlock
                Text="{Binding CurrentPluginText}"
                FontSize="14"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                TextAlignment="Center"
                TextWrapping="Wrap" />
            <ProgressBar
                Height="8"
                IsIndeterminate="{Binding IsIndeterminate}"
                Maximum="{Binding ProgressMaximum}"
                Value="{Binding ProgressValue}" />
            <ProgressRing
                Width="32"
                Height="32"
                HorizontalAlignment="Center"
                IsActive="{Binding IsIndeterminate}" />
        </StackPanel>
    </Grid>
</UserControl>
