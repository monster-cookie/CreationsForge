<Window x:Class="SFRecordCompareEngine.DatabaseImportConfirmationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Initialize Plugin Database"
        Width="420"
        Height="170"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="The plugin database import can take a significant amount of time to complete."
                   TextWrapping="Wrap"
                   VerticalAlignment="Center" />

        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,18,0,0">
            <Button Width="90"
                    Height="28"
                    Margin="0,0,8,0"
                    IsDefault="True"
                    Click="ContinueButton_Click">
                Continue
            </Button>
            <Button Width="90"
                    Height="28"
                    IsCancel="True"
                    Click="ExitButton_Click">
                Exit
            </Button>
        </StackPanel>
    </Grid>
</Window>
