<Window x:Class="SFRecordCompareEngine.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SF Record Compare Engine"
        Height="440"
        Width="640"
        MinHeight="360"
        MinWidth="520">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Database"
                   FontSize="14"
                   FontWeight="SemiBold"
                   Margin="0,0,0,8" />

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <ComboBox x:Name="DatabaseComboBox"
                      IsEditable="True"
                      IsTextSearchEnabled="True"
                      StaysOpenOnEdit="True"
                      MaxDropDownHeight="180"
                      Height="34"
                      VerticalContentAlignment="Center" />

            <Button Grid.Column="1"
                    Content="Load"
                    Width="84"
                    Height="34"
                    Margin="12,0,0,0"
                    Click="LoadButton_Click" />

            <Button Grid.Column="2"
                    Content="Refresh"
                    Width="84"
                    Height="34"
                    Margin="12,0,0,0"
                    Click="RefreshButton_Click" />
        </Grid>

        <TextBlock x:Name="StatusTextBlock"
                   Grid.Row="2"
                   Margin="0,12,0,0"
                   Foreground="#555"
                   TextWrapping="Wrap" />

        <Grid Grid.Row="3" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="110" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Name" FontWeight="SemiBold" Margin="0,0,12,8" />
            <TextBlock x:Name="PluginNameTextBlock" Grid.Row="0" Grid.Column="1" Margin="0,0,0,8" TextWrapping="Wrap" />

            <TextBlock Grid.Row="1" Grid.Column="0" Text="Author" FontWeight="SemiBold" Margin="0,0,12,8" />
            <TextBlock x:Name="PluginAuthorTextBlock" Grid.Row="1" Grid.Column="1" Margin="0,0,0,8" TextWrapping="Wrap" />

            <TextBlock Grid.Row="2" Grid.Column="0" Text="Version" FontWeight="SemiBold" Margin="0,0,12,8" />
            <TextBlock x:Name="PluginVersionTextBlock" Grid.Row="2" Grid.Column="1" Margin="0,0,0,8" TextWrapping="Wrap" />

            <TextBlock Grid.Row="3" Grid.Column="0" Text="Description" FontWeight="SemiBold" Margin="0,0,12,8" />
            <TextBlock x:Name="PluginDescriptionTextBlock" Grid.Row="3" Grid.Column="1" Margin="0,0,0,8" TextWrapping="Wrap" />

            <TextBlock Grid.Row="4" Grid.Column="0" Text="Masters" FontWeight="SemiBold" Margin="0,0,12,0" />
            <TextBlock x:Name="PluginMastersTextBlock" Grid.Row="4" Grid.Column="1" TextWrapping="Wrap" />
        </Grid>
    </Grid>
</Window>
