# Known Issues

1. Currently, the import runs every time the user starts the app. We need to add back the checks for file modification times/sizes to the plugins.
2. Need to add back the file open dialog. To select the active plugin.
3. Once we have an active plugin the tree view needs to be reimplemented with the plugin's major record types.
